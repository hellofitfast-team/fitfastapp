---
phase: 13-page-level-renovation
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05]

must_haves:
  truths:
    - "Workout plan page uses the same DaySelector component (1-14) as the meal plan page"
    - "Exercises are displayed as mini-cards with sets/reps/rest in a 3-column grid"
    - "Muscle group tags appear as colored pills on each exercise card"
    - "Exercise details are collapsible — collapsed shows name + sets/reps summary, expanded shows full breakdown with notes"
    - "A daily workout summary card shows workout type, exercise count, and estimated duration"
    - "Cards appear with stagger animation (50ms delay)"
  artifacts:
    - path: "apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx"
      provides: "Renovated workout plan page with mini-cards, muscle tags, and collapsible details"
      min_lines: 200
  key_links:
    - from: "apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx"
      to: "apps/client/src/app/[locale]/(dashboard)/meal-plan/_components/day-selector.tsx"
      via: "import DaySelector from meal-plan"
      pattern: "import.*DaySelector.*from.*meal-plan"
    - from: "apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx"
      to: "@fitfast/ui/widget-card"
      via: "import for workout summary card"
      pattern: "import.*WidgetCard"
---

<objective>
Renovate the workout plan page with the shared DaySelector, exercise mini-cards with muscle group tags, collapsible exercise details, and a daily workout summary card.

Purpose: Clients check their workout plan daily. Exercises displayed as structured mini-cards with visual muscle tags are far more scannable than a plain list.
Output: Renovated workout plan page matching the meal plan page's design language with exercise-specific card patterns.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-page-level-renovation/13-CONTEXT.md
@.planning/phases/13-page-level-renovation/13-RESEARCH.md
@.planning/phases/13-page-level-renovation/13-02-SUMMARY.md
@apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx
@apps/client/src/app/[locale]/(dashboard)/meal-plan/_components/day-selector.tsx
@packages/ui/src/widget-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renovate workout plan page with DaySelector and daily summary card</name>
  <files>apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx</files>
  <action>
Rewrite the workout plan page JSX while preserving ALL existing data hooks, state, and business logic.

**1. Import DaySelector from meal-plan (WORK-01):**
- Add: `import { DaySelector } from "../meal-plan/_components/day-selector";`
- Replace the existing Mon-Sun weekday selector with: `<DaySelector totalDays={14} selectedDay={selectedDay} onSelectDay={setSelectedDay} planStartDate={workoutPlan?.startDate} featureColor="fitness" />`
- Update state management from weekday-based to 0-indexed day number (0-13), same pattern as meal plan

**2. Daily Workout Summary Card (WORK-05):**
- Below the day selector, add a summary card using `<WidgetCard featureColor="fitness">`
- Inside, show a horizontal stat row: workout type/name (left), exercise count (center), estimated duration (right)
- Each stat: `<div className="text-center flex-1"><p className="text-lg font-bold">{value}</p><p className="text-[10px] text-muted-foreground">{label}</p></div>`
- Separate stats with `<div className="w-px h-8 bg-border" />` vertical dividers

**3. Exercise Mini-Cards with Muscle Tags (WORK-02, WORK-03, WORK-04):**
- Replace the existing `divide-y` exercise list with individual mini-cards
- Each exercise card structure:
  ```
  <div className="rounded-xl border border-border bg-card p-3.5 space-y-3 animate-slide-up" style={{ animationDelay: `${index * 50}ms` }}>
  ```
- **Card header row:** Number badge (01, 02, etc.) in `flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-fitness/10 text-fitness text-xs font-bold` + exercise name as `font-semibold text-sm`
- **Muscle group tags (WORK-03):** Below exercise name, render `exercise.targetMuscles` (or similar field) as colored pills: `rounded-full bg-fitness/10 text-fitness px-2 py-0.5 text-[10px] font-medium`. Wrap in `flex flex-wrap gap-1 mt-1`.
- **Collapsible detail (WORK-04):** Each exercise card is collapsible. Use `expandedExercise` state (`useState<number | null>(null)`). Header click toggles. Add chevron rotation icon.
  - **Collapsed:** Show exercise name + quick summary text like "3x12 reps" inline
  - **Expanded (via max-h transition):** Show the 3-column sets/reps/rest grid:
    ```
    <div className="grid grid-cols-3 gap-2">
      <div className="rounded-lg bg-neutral-50 p-2 text-center">
        <p className="text-sm font-bold">{sets}</p>
        <p className="text-[10px] text-muted-foreground">Sets</p>
      </div>
      ... (Reps, Rest)
    </div>
    ```
  - Also show optional `exercise.notes` in expanded state: `<p className="text-xs text-muted-foreground italic bg-neutral-50 rounded-lg p-2.5">{notes}</p>`
- **Expand animation:** `overflow-hidden transition-all duration-200 ease-in-out` with `max-h-[400px] opacity-100` when expanded, `max-h-0 opacity-0` when collapsed

**4. Warmup/Cooldown sections:**
- Keep existing warmup and cooldown sections if they exist, but apply same card styling (rounded-xl border bg-card)
- These do NOT need collapsible behavior — they're typically short lists

**5. Empty state (EMPTY-03):**
- When no workout plan data exists, render EmptyState with `variant="fitness"` and Dumbbell icon (existing pattern)
  </action>
  <verify>Run `pnpm --filter @fitfast/client build` — no TypeScript errors. Visually confirm at localhost:3000/en/workout-plan that DaySelector shows 14 days with fitness color, exercises display as mini-cards, and muscle group tags appear.</verify>
  <done>Workout plan page uses shared DaySelector (1-14, fitness color), exercises render as collapsible mini-cards with muscle group pills, 3-column sets/reps/rest grid on expand, daily summary card shows workout overview, and stagger animation applied.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @fitfast/client build` passes
2. DaySelector on workout page uses `featureColor="fitness"` (green tint)
3. Exercise mini-cards show numbered badges, name, muscle group tags
4. Clicking an exercise card expands to show sets/reps/rest grid and notes
5. Only one exercise expands at a time
6. Daily workout summary shows type, count, duration
7. Stagger animation visible on card list
</verification>

<success_criteria>
- DaySelector imported from meal-plan/_components/ and working with fitness color
- Exercises displayed as mini-cards (not divide-y list)
- Muscle group tags appear as colored fitness pills
- Collapsible exercise details with 3-col grid for sets/reps/rest
- Daily workout summary card using WidgetCard
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-page-level-renovation/13-03-SUMMARY.md`
</output>
