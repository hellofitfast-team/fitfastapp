---
phase: 15-rtl-audit-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
files_modified:
  - apps/client/src/app/[locale]/(dashboard)/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/meal-plan/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/tickets/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/tickets/[id]/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/tracking/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/progress/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/settings/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/faq/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/check-in/page.tsx
  - apps/client/src/components/layouts/dashboard-shell.tsx
autonomous: false
requirements:
  - VERIFY-01
  - VERIFY-03

must_haves:
  truths:
    - "Every renovated page renders correctly in Arabic at 390px mobile viewport"
    - "Every renovated page renders correctly in Arabic at 1440px desktop viewport"
    - "PWA standalone mode works with safe areas and no overlapping browser chrome"
    - "No functionality regressions from v1.0 — all features work identically"
  artifacts:
    - path: "apps/client/src/app/[locale]/(dashboard)/page.tsx"
      provides: "Home page verified in Arabic"
    - path: "apps/client/src/components/layouts/dashboard-shell.tsx"
      provides: "Shell layout with safe area handling verified"
  key_links:
    - from: "dashboard-shell.tsx"
      to: "bottom-nav.tsx"
      via: "safe-area-inset-bottom in padding calculation"
      pattern: "env\\(safe-area-inset-bottom\\)"
    - from: "apps/client/public/manifest.json"
      to: "PWA standalone display"
      via: "display: standalone"
      pattern: "standalone"
---

<objective>
Run a comprehensive regression check across all renovated pages, verify Arabic visual correctness at 390px and 1440px viewports, and test PWA standalone mode with safe areas.

Purpose: Final quality gate before shipping v1.1 — ensure RTL fixes from plans 01/02 are correct and no functionality was broken.
Output: All pages verified, any remaining issues found and fixed inline, regression check complete.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-rtl-audit-polish/15-RESEARCH.md
@.planning/phases/15-rtl-audit-polish/15-01-SUMMARY.md
@.planning/phases/15-rtl-audit-polish/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated RTL and regression audit across all renovated pages</name>
  <files>
    apps/client/src/app/[locale]/(dashboard)/page.tsx
    apps/client/src/app/[locale]/(dashboard)/meal-plan/page.tsx
    apps/client/src/app/[locale]/(dashboard)/workout-plan/page.tsx
    apps/client/src/app/[locale]/(dashboard)/tickets/page.tsx
    apps/client/src/app/[locale]/(dashboard)/tickets/[id]/page.tsx
    apps/client/src/app/[locale]/(dashboard)/tracking/page.tsx
    apps/client/src/app/[locale]/(dashboard)/progress/page.tsx
    apps/client/src/app/[locale]/(dashboard)/settings/page.tsx
    apps/client/src/app/[locale]/(dashboard)/faq/page.tsx
    apps/client/src/app/[locale]/(dashboard)/check-in/page.tsx
    apps/client/src/components/layouts/dashboard-shell.tsx
  </files>
  <action>
    Run a thorough automated audit of all renovated component files:

    **Step 1 — Physical property sweep:**
    Grep ALL .tsx files under `apps/client/src/` for remaining physical CSS properties that should be logical:
    - `ml-` (should be `ms-`), `mr-` (should be `me-`), `pl-` (should be `ps-`), `pr-` (should be `pe-`)
    - `left-` (should be `start-`), `right-` (should be `end-`)
    - `text-left` (should be `text-start`), `text-right` (should be `text-end`)
    - `border-l-` (should be `border-s-`), `border-r-` (should be `border-e-`)
    - `rounded-l` (should be `rounded-s`), `rounded-r` (should be `rounded-e`)

    **Exceptions that are OK (do NOT convert):**
    - `left-0 right-0` already converted to `inset-x-0` in 15-01
    - `rounded-lg`, `rounded-xl`, `rounded-2xl` (these are all-corners, not directional)
    - Physical properties inside `cn()` conditionals that already have RTL handling (like the `isRtl` check in tickets)
    - `translate-x`, `scale-x` (transforms, not layout)
    - Radix/shadcn animation classes like `slide-in-from-left` (component library handles RTL internally)

    Fix any remaining physical properties found in the dashboard pages by converting to logical equivalents.

    **Step 2 — Progress bar direction verification:**
    Check all progress bars have `dir` attribute for correct fill direction in RTL:
    - Home page progress bar (~line 383): Has `dir={locale === "ar" ? "rtl" : "ltr"}` — verify still present
    - Plan countdown bar (~line 252): Has `dir` attribute — verify still present
    - Check-in step progress: Uses flexbox `flex-1` for connectors — direction-neutral, OK
    - If any progress bar is missing `dir` attribute, add it

    **Step 3 — Build verification:**
    Run `pnpm --filter @fitfast/client build` and fix any TypeScript errors.

    **Step 4 — PWA manifest verification:**
    Read `apps/client/public/manifest.json` and verify:
    - `"display": "standalone"` is present
    - `"dir": "auto"` is present (respects page direction)
    - `"orientation": "portrait-primary"` is present
    Read `apps/client/src/app/layout.tsx` and verify:
    - `viewportFit: "cover"` in viewport export (enables safe area insets)
    - `appleWebApp.capable: true` in metadata

    **Step 5 — Safe area verification:**
    Verify safe area handling in layout:
    - `dashboard-shell.tsx`: Main content padding includes `env(safe-area-inset-bottom)` — verify present
    - `bottom-nav.tsx`: Uses `mb-[max(0.5rem,env(safe-area-inset-bottom))]` — verify present
    - Both should use `max()` to avoid zero-margin on non-notch devices

    Fix any issues found during the audit inline.
  </action>
  <verify>
    Run `grep -rn "ml-\[" apps/client/src/app/\[locale\]/\(dashboard\)/ --include="*.tsx" | grep -v "node_modules"` — should return zero results for physical margin-left with arbitrary values.
    Run `pnpm --filter @fitfast/client build` — should succeed with no errors.
    Run `grep -n "standalone" apps/client/public/manifest.json` — should find display: standalone.
    Run `grep -n "safe-area-inset" apps/client/src/components/layouts/dashboard-shell.tsx apps/client/src/components/layouts/bottom-nav.tsx` — should find env() usage in both files.
  </verify>
  <done>
    All renovated pages pass automated RTL property audit, build succeeds, PWA manifest is correct, safe areas are handled, no physical CSS properties remain in layout code.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification in Arabic at 390px and 1440px</name>
  <what-built>
    All RTL fixes from plans 15-01 and 15-02 have been applied, and plan 15-03 Task 1 has completed an automated audit. The app should now render correctly in Arabic RTL mode at both mobile and desktop viewports.
  </what-built>
  <how-to-verify>
    Start the dev server: `pnpm dev:client`

    **Arabic 390px Mobile Verification (use browser DevTools mobile simulation):**
    1. Navigate to `http://localhost:3000/ar` — Home page should show Arabic greeting, right-aligned text, carousel scrolls from right
    2. Navigate to `http://localhost:3000/ar/meal-plan` — DaySelector shows Arabic day labels ("يوم 1"), scrolls from right edge
    3. Navigate to `http://localhost:3000/ar/workout-plan` — Same DaySelector behavior, exercise cards right-aligned
    4. Navigate to `http://localhost:3000/ar/tickets` — Ticket list items right-aligned, chevrons point left
    5. Open any ticket — Chat bubbles: client messages on LEFT (blue), coach messages on RIGHT (gray) — this is the RTL-inverted layout. Send icon should point left.
    6. Navigate to `http://localhost:3000/ar/check-in` — Step progress bar renders correctly, back/next arrows are flipped
    7. Navigate to `http://localhost:3000/ar/tracking` — Cards right-aligned
    8. Navigate to `http://localhost:3000/ar/progress` — Photos grid, charts render correctly
    9. Navigate to `http://localhost:3000/ar/settings` — Form labels right-aligned
    10. Navigate to `http://localhost:3000/ar/faq` — Questions expand correctly, search icon on right side

    **Arabic 1440px Desktop Verification (full browser window):**
    11. Same pages as above but at desktop width — top navbar should show Arabic nav items right-to-left
    12. Content should be centered with max-width, grid layouts should be 2-3 columns
    13. No horizontal overflow or layout breaks

    **PWA Standalone Test:**
    14. On an iOS device or simulator: Add to Home Screen from Safari, launch as standalone app
    15. Verify no browser chrome overlaps content, bottom nav has safe area spacing
    16. OR: In Chrome DevTools > Application > Manifest — verify "display: standalone" and no errors

    **Expected behavior:** All text right-aligned, icons mirrored where appropriate, progress bars fill from right, no layout breaks or overlapping elements.
  </how-to-verify>
  <resume-signal>Type "approved" if all pages look correct in Arabic at both viewports, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Automated RTL property audit passes — zero physical left/right in layout code
2. Build succeeds with no TypeScript errors
3. Human verification confirms Arabic pages look correct at 390px and 1440px
4. PWA standalone mode verified (safe areas, no chrome overlap)
5. All features work identically to v1.0 (no regressions)
</verification>

<success_criteria>
- Every renovated page verified in Arabic at 390px mobile viewport
- Every renovated page verified in Arabic at 1440px desktop viewport
- PWA standalone mode works with safe areas
- Zero physical left/right CSS properties in dashboard layout code
- Build passes
- Human approval received
</success_criteria>

<output>
After completion, create `.planning/phases/15-rtl-audit-polish/15-03-SUMMARY.md`
</output>
