---
phase: 15-rtl-audit-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/layouts/mobile-header.tsx
  - apps/client/src/components/auth/LocaleSwitcher.tsx
  - apps/client/src/app/[locale]/(dashboard)/faq/page.tsx
  - apps/client/src/app/[locale]/(dashboard)/progress/_components/photos-tab.tsx
  - apps/client/src/app/[locale]/(dashboard)/page.tsx
autonomous: true
requirements:
  - VERIFY-02
  - RTL-08

must_haves:
  truths:
    - "All interactive elements have minimum 44px touch targets on mobile"
    - "Mobile header action icons are 44x44px hit areas"
    - "FAQ expand/collapse buttons have 44px minimum touch area"
    - "Photo modal close button has 44px minimum touch area"
    - "Locale switcher button is 44x44px"
  artifacts:
    - path: "apps/client/src/components/layouts/mobile-header.tsx"
      provides: "44px touch targets for header action icons"
      contains: "h-11 w-11"
    - path: "apps/client/src/components/auth/LocaleSwitcher.tsx"
      provides: "44px locale switcher touch target"
      contains: "h-11 w-11"
    - path: "apps/client/src/app/[locale]/(dashboard)/faq/page.tsx"
      provides: "FAQ question rows have adequate touch area"
      contains: "min-h-[44px]"
    - path: "apps/client/src/app/[locale]/(dashboard)/progress/_components/photos-tab.tsx"
      provides: "Photo modal close button 44px"
      contains: "h-11 w-11"
  key_links:
    - from: "mobile-header.tsx"
      to: "all dashboard pages"
      via: "DashboardShell renders MobileHeader"
      pattern: "MobileHeader"
---

<objective>
Ensure all interactive elements meet the 44px minimum touch target requirement and fix any remaining visual RTL issues for Arabic mode verification at 390px and 1440px viewports.

Purpose: Meet WCAG touch target guidelines and ensure Arabic users can comfortably interact with all controls.
Output: Five files updated with 44px minimum touch targets on all interactive elements.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-rtl-audit-polish/15-RESEARCH.md
@apps/client/src/components/layouts/mobile-header.tsx
@apps/client/src/components/auth/LocaleSwitcher.tsx
@apps/client/src/app/[locale]/(dashboard)/faq/page.tsx
@apps/client/src/app/[locale]/(dashboard)/progress/_components/photos-tab.tsx
@apps/client/src/app/[locale]/(dashboard)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix touch targets across mobile header, locale switcher, and FAQ</name>
  <files>
    apps/client/src/components/layouts/mobile-header.tsx
    apps/client/src/components/auth/LocaleSwitcher.tsx
    apps/client/src/app/[locale]/(dashboard)/faq/page.tsx
  </files>
  <action>
    1. **mobile-header.tsx — Increase action icon touch targets from 36px to 44px**:
       - Lines ~99, ~108, ~118: Change all `h-9 w-9` (36px) icon buttons to `h-11 w-11` (44px). These are the locale switcher, settings gear, and notification bell buttons in the mobile header.
       - The icon sizes inside (h-4 w-4 or h-5 w-5) should stay the same — only the touch target container grows.
       - Example: `className="flex h-9 w-9 items-center justify-center rounded-lg..."` becomes `className="flex h-11 w-11 items-center justify-center rounded-lg..."`
       - Do NOT change the user avatar circle at line ~70 (`h-9 w-9 rounded-full bg-primary`) — it's decorative, not an interactive button.

    2. **LocaleSwitcher.tsx — Increase from 40px to 44px**:
       - Line ~20: Change `h-10 w-10` (40px) to `h-11 w-11` (44px).
       - This is a minimal change but ensures compliance.

    3. **faq/page.tsx — Ensure FAQ question buttons meet 44px**:
       - The FAQ question button (line ~80-98) already uses `w-full p-4` which gives adequate height. BUT the inner number badge is `h-8 w-8` (32px). This badge is NOT the touch target (the entire row is), so it's fine.
       - Add `min-h-[44px]` to the FAQ question `<button>` element on line ~82 to explicitly guarantee the touch target:
         ```tsx
         className="w-full p-4 flex items-start justify-between gap-3 text-start hover:bg-neutral-50 transition-colors min-h-[44px]"
         ```
       - The "Contact Support" button at the bottom (line ~131) already has `px-6 py-2.5` which gives ~40px height. Increase to `py-3` for 44px+ height.
  </action>
  <verify>
    Run `grep -n "h-9 w-9" apps/client/src/components/layouts/mobile-header.tsx` — should only appear on the non-interactive avatar div, not on buttons.
    Run `grep -n "h-10 w-10" apps/client/src/components/auth/LocaleSwitcher.tsx` — should return zero results.
    Run `grep -n "min-h-\[44px\]" apps/client/src/app/\[locale\]/\(dashboard\)/faq/page.tsx` — should find the FAQ button.
  </verify>
  <done>
    All mobile header action icons are 44x44px, locale switcher is 44x44px, FAQ buttons have min-h-[44px], contact support button has adequate height.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix photo modal close button and carousel dot touch targets</name>
  <files>
    apps/client/src/app/[locale]/(dashboard)/progress/_components/photos-tab.tsx
    apps/client/src/app/[locale]/(dashboard)/page.tsx
  </files>
  <action>
    1. **photos-tab.tsx — Photo modal close button**:
       - Line ~67-69: The close button uses `h-9 w-9` (36px). Change to `h-11 w-11` (44px):
         ```tsx
         className="h-11 w-11 rounded-lg flex items-center justify-center hover:bg-neutral-100 transition-colors"
         ```
       - Keep the X icon at `h-5 w-5` — only the container changes.

    2. **page.tsx (home) — Carousel dot indicators**:
       - Lines ~332-339: The dot indicators are `h-1.5 w-1.5`/`w-4` (6px/16px) which are far too small for touch interaction.
       - Wrap each dot in a larger touch target area. The visual dot stays small, but the tappable area is 44px:
         ```tsx
         <div className="flex justify-center gap-0 mt-3">
           {[0, 1, 2, 3].map((i) => (
             <button
               key={i}
               onClick={() => { scrollToIndex(i); setActiveCardIndex(i); }}
               className="flex items-center justify-center h-11 w-11"
               aria-label={`Go to card ${i + 1}`}
             >
               <span
                 className={cn(
                   "h-1.5 rounded-full transition-all duration-300",
                   activeCardIndex === i ? "w-4 bg-primary" : "w-1.5 bg-neutral-300"
                 )}
               />
             </button>
           ))}
         </div>
         ```
       - This keeps the visual appearance identical (small dots) but wraps them in 44x44px touch targets.
       - Add `aria-label` for accessibility.
  </action>
  <verify>
    Run `grep -n "h-9 w-9" apps/client/src/app/\[locale\]/\(dashboard\)/progress/_components/photos-tab.tsx` — should return zero results (close button now h-11 w-11).
    Run `grep -n "aria-label" apps/client/src/app/\[locale\]/\(dashboard\)/page.tsx` — should find aria-label on carousel dots.
    Run `pnpm --filter @fitfast/client build` to verify no TypeScript errors.
  </verify>
  <done>
    Photo modal close button is 44x44px, carousel dots have 44x44px touch targets with aria-labels, build passes.
  </done>
</task>

</tasks>

<verification>
1. All interactive elements in mobile header, locale switcher, FAQ, photo modal, and carousel dots have minimum 44px touch targets
2. `pnpm --filter @fitfast/client build` succeeds
3. No visual regression — icon sizes inside buttons remain unchanged, only outer containers grow
</verification>

<success_criteria>
- Mobile header action icons: h-11 w-11 (44px)
- Locale switcher: h-11 w-11 (44px)
- FAQ question buttons: min-h-[44px]
- Photo modal close: h-11 w-11 (44px)
- Carousel dots: wrapped in h-11 w-11 touch targets with aria-labels
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-rtl-audit-polish/15-02-SUMMARY.md`
</output>
