---
phase: 11-foundation-shell-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/layouts/sidebar.tsx
  - apps/client/src/components/layouts/dashboard-shell.tsx
  - apps/client/src/components/layouts/header.tsx
  - apps/client/src/components/layouts/index.ts
  - apps/client/src/components/layouts/more-menu.tsx
autonomous: true
requirements: [NAV-02, NAV-03, NAV-04, DS-05, DESK-01]

must_haves:
  truths:
    - "Old sidebar.tsx, dashboard-shell.tsx (v1), and header.tsx are deleted"
    - "Barrel export in index.ts exports only v2 components"
    - "The app builds and runs without import errors after cleanup"
    - "More menu bottom sheet has smooth slide-up animation"
  artifacts:
    - path: "apps/client/src/components/layouts/index.ts"
      provides: "Clean barrel exports for v2 layout only"
    - path: "apps/client/src/components/layouts/mobile-header.tsx"
      provides: "Mobile header (already exists, preserved)"
    - path: "apps/client/src/components/layouts/desktop-top-nav.tsx"
      provides: "Desktop top nav (already exists, preserved)"
    - path: "apps/client/src/components/layouts/more-menu.tsx"
      provides: "More menu bottom sheet (already exists, preserved)"
  key_links:
    - from: "apps/client/src/app/[locale]/(dashboard)/layout.tsx"
      to: "apps/client/src/components/layouts/index.ts"
      via: "DashboardShell import"
      pattern: "import.*DashboardShell.*from.*@/components/layouts"
---

<objective>
Remove deprecated v1 layout files (sidebar, old header, old dashboard-shell) and clean up barrel exports.

Purpose: The v2 shell is already active (barrel export points to dashboard-shell-v2). The old files are dead code that could cause confusion. Cleaning up completes the shell migration.
Output: Clean layouts directory with only v2 components.
</objective>

<execution_context>
@/Users/ziadadel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ziadadel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/client/src/components/layouts/index.ts
@apps/client/src/app/[locale]/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deprecated v1 layout files and clean barrel exports</name>
  <files>apps/client/src/components/layouts/sidebar.tsx, apps/client/src/components/layouts/dashboard-shell.tsx, apps/client/src/components/layouts/header.tsx, apps/client/src/components/layouts/index.ts</files>
  <action>
1. First, verify no other files import `Sidebar`, `Header` (from layouts), or `dashboard-shell` (v1) anywhere in the codebase:
   ```bash
   grep -r "from.*layouts.*sidebar\|from.*layouts.*header\|dashboard-shell[^-]" apps/client/src/ --include="*.ts" --include="*.tsx"
   ```
   The barrel export `index.ts` currently exports `Header` and `Sidebar` but the dashboard-shell-v2 does NOT use them. If any other file imports them, update those imports or note it.

2. Delete the deprecated files:
   - `apps/client/src/components/layouts/sidebar.tsx` (replaced by bottom-nav + more-menu)
   - `apps/client/src/components/layouts/dashboard-shell.tsx` (replaced by dashboard-shell-v2)
   - `apps/client/src/components/layouts/header.tsx` (replaced by mobile-header)

3. Update `apps/client/src/components/layouts/index.ts` to only export v2 components:
   ```typescript
   export { DashboardShell } from "./dashboard-shell-v2";
   export { MobileHeader } from "./mobile-header";
   export { DesktopTopNav } from "./desktop-top-nav";
   export { BottomNav } from "./bottom-nav";
   export { MoreMenu } from "./more-menu";
   ```

4. Rename `dashboard-shell-v2.tsx` to `dashboard-shell.tsx` and update the barrel export accordingly. This removes the "-v2" suffix now that there's no v1 to distinguish from. Update the import path in `index.ts` to `"./dashboard-shell"`.
  </action>
  <verify>Run `pnpm --filter @fitfast/client build` — build succeeds with no missing import errors. Run `ls apps/client/src/components/layouts/` and confirm sidebar.tsx, header.tsx, and dashboard-shell.tsx (old v1) are gone. The new dashboard-shell.tsx (renamed from v2) exists.</verify>
  <done>Only v2 layout components remain. No dead code. Build passes. The layouts directory contains: dashboard-shell.tsx, mobile-header.tsx, bottom-nav.tsx, desktop-top-nav.tsx, more-menu.tsx, index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all navigation requirements are met</name>
  <files>apps/client/src/components/layouts/more-menu.tsx</files>
  <action>
Run a final requirements audit by checking each requirement against the codebase:

1. **NAV-01** (pill-shaped bottom nav): Confirm bottom-nav.tsx has `rounded-2xl`, horizontal margin, and shadow classes. If plan 11-01 hasn't run yet, note this as a dependency.
2. **NAV-02** (desktop top navbar): Confirm desktop-top-nav.tsx renders at `lg+` with all 9 nav items.
3. **NAV-03** (mobile header): Confirm mobile-header.tsx shows page title + action icons, no hamburger menu (no `Menu` icon import).
4. **NAV-04** (More bottom sheet): Confirm more-menu.tsx uses Drawer from vaul with Tracking, Progress, Tickets, FAQ items.
5. **NAV-05** (active states): Confirm bottom-nav.tsx and desktop-top-nav.tsx both have active state styling (primary color, indicator dot/highlight).
6. **NAV-07** (keyboard hiding): Confirm bottom-nav.tsx uses `useKeyboardVisible` and returns null when keyboard is open.
7. **DS-04** (safe areas): Confirm viewport-fit=cover in root layout.tsx and env(safe-area-inset-*) usage in bottom-nav and more-menu.
8. **DS-05** (animations): Confirm globals.css has fadeIn, slideUp, slideDown, scaleIn keyframes and animate-* tokens.
9. **DESK-01** (desktop top nav): Same as NAV-02.

Fix the duplicate `pb-` classes in more-menu.tsx line 50 (`pb-6 pb-[env(safe-area-inset-bottom)]` should be `pb-[max(1.5rem,env(safe-area-inset-bottom))]`).

If any requirement is not met, fix it in this task. If all pass, no changes needed beyond the more-menu fix.
  </action>
  <verify>Run `pnpm --filter @fitfast/client build` — passes. All 10 requirements (NAV-01 through NAV-07, DS-04, DS-05, DESK-01) are confirmed met by code inspection.</verify>
  <done>All Phase 11 requirements verified in code. No missing functionality. Build passes cleanly.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @fitfast/client build` passes
- `pnpm type-check` passes
- No imports of deleted files remain
- Layouts directory contains exactly: dashboard-shell.tsx, mobile-header.tsx, bottom-nav.tsx, desktop-top-nav.tsx, more-menu.tsx, index.ts
- All 10 requirements (NAV-01..07, DS-04, DS-05, DESK-01) confirmed in code
</verification>

<success_criteria>
- Zero dead code in layouts directory
- App builds and runs with only v2 shell components
- All Phase 11 requirements verified as implemented
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation-shell-navigation/11-02-SUMMARY.md`
</output>
